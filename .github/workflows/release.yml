name: Build Home Assistant app

on:
  repository_dispatch:
    types:
      - release-built
  workflow_dispatch:
    inputs:
      version:
        description: Pulse2MQTT version
        required: true
        type: string
      tag:
        description: Pulse2MQTT release tag, for example v0.5.0 or edge
        required: true
        default: edge
        type: string
      channel:
        description: Image channel to publish
        required: true
        default: edge
        type: choice
        options:
          - edge
          - latest

permissions:
  contents: read
  packages: write

concurrency:
  group: app-${{ github.event.client_payload.version || inputs.version }}-${{ github.event.client_payload.channel || inputs.channel }}
  cancel-in-progress: false

env:
  APP_IMAGE_DOCKERHUB: docker.io/snislab/app-pulse2mqtt
  APP_IMAGE_GHCR: ghcr.io/snislab/app-pulse2mqtt
  SOURCE_REPOSITORY: SnisLab/pulse2mqtt

jobs:
  release:
    name: Publish app
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Determine build inputs
        id: release
        shell: bash
        env:
          EVENT_CHANNEL: ${{ github.event.client_payload.channel }}
          EVENT_TAG: ${{ github.event.client_payload.tag }}
          EVENT_VERSION: ${{ github.event.client_payload.version }}
          INPUT_CHANNEL: ${{ inputs.channel }}
          INPUT_TAG: ${{ inputs.tag }}
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          version="${EVENT_VERSION:-${INPUT_VERSION}}"
          tag="${EVENT_TAG:-${INPUT_TAG}}"
          channel="${EVENT_CHANNEL:-${INPUT_CHANNEL}}"
          if [[ -z "${channel}" ]]; then
            if [[ "${tag}" == "edge" ]]; then
              channel=edge
            else
              channel=latest
            fi
          fi
          test -n "${version}"
          test -n "${tag}"
          [[ "${version}" =~ ^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]
          [[ "${tag}" == "edge" || "${tag}" == v* ]]
          [[ "${channel}" == "edge" || "${channel}" == "latest" ]]
          echo "version=${version}" >> "${GITHUB_OUTPUT}"
          echo "tag=${tag}" >> "${GITHUB_OUTPUT}"
          echo "channel=${channel}" >> "${GITHUB_OUTPUT}"

          if [[ "${channel}" == "latest" ]]; then
            printf 'latest\n%s\n' "${version}" > "${GITHUB_OUTPUT}.tags"
          else
            printf 'edge\n' > "${GITHUB_OUTPUT}.tags"
          fi

      - name: Download Pulse2MQTT binaries
        shell: bash
        env:
          RELEASE_TAG: ${{ steps.release.outputs.tag }}
        run: |
          set -euo pipefail
          mkdir -p binaries/amd64 binaries/arm64
          for arch in amd64 arm64; do
            archive="pulse2mqtt_linux_${arch}.tar.gz"
            url="https://github.com/${SOURCE_REPOSITORY}/releases/download/${RELEASE_TAG}/${archive}"
            curl --fail --location --retry 3 "${url}" --output "${RUNNER_TEMP}/${archive}"
            mkdir -p "${RUNNER_TEMP}/pulse2mqtt-${arch}"
            tar -xzf "${RUNNER_TEMP}/${archive}" -C "${RUNNER_TEMP}/pulse2mqtt-${arch}"
            binary="$(find "${RUNNER_TEMP}/pulse2mqtt-${arch}" -type f -name pulse2mqtt -print -quit)"
            test -n "${binary}"
            cp "${binary}" "binaries/${arch}/pulse2mqtt"
          done

      - name: Run Home Assistant app linter
        uses: frenck/action-app-linter@v2
        with:
          path: .

      - name: Set up QEMU
        uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and publish app image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            BUILD_VERSION=${{ steps.release.outputs.version }}
          tags: |
            ${{ env.APP_IMAGE_DOCKERHUB }}:${{ steps.release.outputs.channel }}
            ${{ env.APP_IMAGE_GHCR }}:${{ steps.release.outputs.channel }}
            ${{ env.APP_IMAGE_DOCKERHUB }}:${{ steps.release.outputs.version }}
            ${{ env.APP_IMAGE_GHCR }}:${{ steps.release.outputs.version }}
